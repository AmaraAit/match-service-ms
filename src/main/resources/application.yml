server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bdd_football
    username: root
    password: 
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
  application:
    name: MATCHS-SERVICE
